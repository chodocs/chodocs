[build.environment]
  NODE_VERSION = "16"

[build]
  publish = "dist"
  command = "npx pnpm i --store=node_modules/.pnpm-store && pnpm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200